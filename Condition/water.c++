#include <bits/stdc++.h>
using namespace std;

// void speed(){
//     ios_base::sync_with_stdio(false);
//     cin.tie(nullptr);
//     cout.tie(nullptr);
// }

int main(){
  //  speed(); // Call speed() at the beginning
  int a;
  cin>>a;
  if(a%2==0 && a>=4)
  cout<<"YES";
    
    else
    cout<<"NO";
}